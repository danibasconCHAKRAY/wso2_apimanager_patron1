mysql -u root -proot apimgtdb < mysql-api5.7.sql
mysql -u root -proot userdb < mysql5.7.sql
mysql -u root -proot regdb < mysql5.7.sql
mysql -u root -proot statdb < mysql5.7.sql
mysql -u root -proot mbstoredb < mysql-mb.sql
